# 工程结构（当前）

更新时间：2026-07-24（Asia/Shanghai）

## 顶层结构
```text
<项目目录>
├─ README.md                              # GitHub 仓库说明与运行/打包入口
├─ GIT_GITHUB_WORKFLOW.md                 # Git/GitHub 分支、提交、推送、发行协作流程
├─ .gitignore                             # Git 忽略规则（本机依赖、发行版、临时文件）
├─ .gitattributes                         # Git 文本换行与二进制文件规则
├─ .github\pull_request_template.md       # GitHub PR 检查模板
├─ vmd_style_tool_qt6.py                  # Qt6 主界面程序
├─ vmd_style_tool.py                      # 核心风格、共用 VMD Tcl 与脚本生成逻辑
├─ direct_workflow_qt6.py                 # 直接绘图、文件拖放、进程监测与结果目录控制
├─ style_parameter_dialog_qt6.py          # VMD 风格参数查看与自定义编辑对话框
├─ multiwfn_batch_qt6.py                  # 批量 Multiwfn 工作台界面
├─ multiwfn_batch.py                      # 批处理模板、队列执行、输出归档与汇总核心
├─ multiwfn_recorder_qt6.py                # 内嵌 Multiwfn 交互试录、命令记录与输出识别
├─ vmd_custom_styles.default.json         # 发行版首次启动使用的空白自定义风格模板
├─ vmd_custom_styles.json                 # 本机用户自定义风格数据（Git 忽略）
├─ STYLE_PARAMETER_AUDIT_20260720.md      # 内置风格与原文/附带脚本的只读核验报告
├─ vmd_style_tool_config.json             # 本机路径与最近配置
├─ multiwfn_batch_presets.json            # 运行时生成的用户批处理模板（Git 忽略）
├─ requirements.txt                       # 固定的运行依赖版本
├─ requirements-build.txt                 # 固定的打包依赖版本
├─ tests\
│  ├─ test_core.py                        # 核心逻辑、脚本生成与配置测试
│  ├─ test_multiwfn_batch.py               # 批处理模板、扫描、执行隔离与输出测试
│  └─ test_qt_smoke.py                    # Qt 界面与批量工作台冒烟测试
├─ AutoCube_OneClick.cmd                 # 基础脚本模板参考
├─ build_release.ps1                      # 可移植的一键打包脚本
├─ cleanup_project.ps1                    # 发行版保留、缓存和临时脚本安全清理工具
├─ append_sync_log.ps1                    # UTF-8 协作日志追加工具
├─ vmd_cube_styles\                       # 风格封面图资源目录
├─ batch_runs\                            # 默认批处理运行记录与结果（运行时生成、Git 忽略）
├─ release\<日期或日期_vN>\
│  ├─ VMD_Multiwfn_StyleGenerator.exe     # 当日保留的最新发行版
│  ├─ vmd_cube_styles\                    # 首次运行后生成的内置资源
│  ├─ vmd_custom_styles.json              # 首次运行后生成的本机用户数据
│  └─ vmd_style_tool_config.json          # 运行后生成的本机配置
├─ Multiwfn_2026.7.11_bin_Win64\          # 当前本机 Multiwfn 目录（用于测试）
├─ vmd19.3\                               # 本机 VMD 目录（用于测试）
├─ 工程说明_协作规范.md                    # 协作规则
├─ 工程结构.md                            # 结构说明（本文件）
└─ THREAD_SYNC_LOG.md                     # 统一协作日志
```

## 约定
- 新增/删除文件时，必须同步更新本文件。
- 仅当发行方式变化（如改回 onedir）时，更新 `release` 节点说明。
