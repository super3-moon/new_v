# 工程结构（当前）

更新时间：2026-04-11（Asia/Shanghai）

## 顶层结构
```text
E:\test
├─ README.md                              # GitHub 仓库说明与运行/打包入口
├─ .gitignore                             # Git 忽略规则（本机依赖、发行版、临时文件）
├─ .gitattributes                         # Git 文本换行与二进制文件规则
├─ vmd_style_tool_qt6.py                  # Qt6 主界面程序
├─ vmd_style_tool.py                      # 核心风格与脚本生成逻辑
├─ vmd_custom_styles.json                 # 自定义风格数据
├─ vmd_style_tool_config.json             # 本机路径与最近配置
├─ AutoCube_OneClick.cmd                 # 基础脚本模板参考
├─ vmd_cube_styles\                       # 风格封面图资源目录
├─ release\
│  ├─ VMD_Multiwfn_StyleGenerator.exe     # 当前发行版（单文件）
│  ├─ vmd_cube_styles\                     # 运行后自动生成/缓存
│  ├─ vmd_custom_styles.json              # 运行后自动生成/缓存
│  └─ vmd_style_tool_config.json          # 运行后自动生成/缓存
├─ Multiwfn_2026.3.27_bin_Win64\          # 本机 Multiwfn 目录（用于测试）
├─ vmd19.3\                               # 本机 VMD 目录（用于测试）
├─ 工程说明_协作规范.md                    # 协作规则
├─ 工程结构.md                            # 结构说明（本文件）
└─ THREAD_SYNC_LOG.md                     # 统一协作日志
```

## 约定
- 新增/删除文件时，必须同步更新本文件。
- 仅当发行方式变化（如改回 onedir）时，更新 `release` 节点说明。
