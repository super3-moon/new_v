# 工程说明与协作规范

## 1. 工程目标
- 本工程用于生成 VMD + Multiwfn 的自动脚本（`.cmd`），支持：
- 套装模式
- 拆分模式（骨架/等值面分开选择）
- VMD Save State 自定义风格导入
- GUI 为 PySide6（Qt6）原生桌面界面。

## 2. 关键文件（必须保留）
- `vmd_style_tool_qt6.py`：Qt6 主界面与交互逻辑。
- `vmd_style_tool.py`：风格数据、脚本生成核心逻辑。
- `vmd_cube_styles/`：风格封面图片资源。
- `vmd_custom_styles.json`：用户自定义风格存储。
- `release/VMD_Multiwfn_StyleGenerator.exe`：当前发行版可执行文件。

## 3. 协作同步文件（根目录）
- `工程说明_协作规范.md`：本文件，记录协作规则和关键约束。
- `工程结构.md`：当前工程结构与每个文件用途。
- `THREAD_SYNC_LOG.md`：所有线程统一写入的日志文件（单一事实来源）。

## 4. 多线程协作规则（强制）
1. 开始改动前：先在 `THREAD_SYNC_LOG.md` 写一条 `START`。
2. 修改过程中：如果新增/删除了文件，必须同步更新 `工程结构.md`。
3. 修改完成后：在 `THREAD_SYNC_LOG.md` 写一条 `DONE`，列出改动文件和结果。
4. 若阻塞：写 `BLOCKED`，说明原因和下一步。
5. 不允许“只改代码不写日志”。

## 5. 推荐写日志方式
- 使用根目录脚本 `append_sync_log.ps1` 追加日志，减少并发写冲突。
- 示例：
```powershell
powershell -ExecutionPolicy Bypass -File .\append_sync_log.ps1 `
  -Thread "packaging" `
  -Phase "START" `
  -Files "vmd_style_tool_qt6.py" `
  -Summary "开始打包发行版"
```

## 6. 额外建议（提高一致性）
- 最佳实践仍是 Git 分支协作：开发线程与打包线程分支隔离。
- 打包线程只从“明确版本提交”打包，避免打包时源码仍在变化。
- 每次发布在日志中写明版本号或时间戳。

## 7. 发行版线程约定（新增）
- 发行版线程只做打包，不做业务功能改动（除非明确要求）。
- 发行目录固定为 `E:\test\release`。
- 每次发行在该目录下创建日期子目录：
- `YYYY-MM-DD` 或 `YYYY-MM-DD_v2`。
- 可使用 `build_release.ps1` 统一执行打包流程。
- 发行策略详见 `RELEASE_POLICY.md`。
